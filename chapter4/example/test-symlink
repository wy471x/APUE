#include "../../include/apue.h"
void test1(){
    if(symlink("actualpath", "sympath") < 0)
        err_sys(" symlink error.");
    printf("symlink success.\n");
}
int main(void)
{
    test1();
    return 0;
}

